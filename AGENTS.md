使用 GitHub Actions 进行跨平台开发和测试。

开发或测试时不要在 main 分支上直接变动。可以按照规范使用 git 多次提交：小批量高频集成，这是为了使用 CI 进行测试。
需要合并时提 PR。PR 如果被合并就新开分支，维持历史整洁，可以积极使用 cherry-pick & rebase，但不要破坏已提交历史。

对于涉及到的 C/C++ 开发，使用 C++23 和 modern CMake，配置好构建工具链和 presets。

不要有狭隘的 MVP 思维和规划，AI-native 的成本模型完全不一致，应当做好数学建模和系统设计自顶向下设计实现，原生产品化，考虑用户与开发者体验。
