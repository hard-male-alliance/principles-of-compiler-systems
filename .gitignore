# Local editor state / 本地编辑器状态
.idea/
.vscode/

# Research and experiment caches / 调研与实验缓存
.cache/
.temp/

# CMake and generated compiler artifacts / CMake 与编译器生成物
preflight/build*/
preflight/out/
*.o
*.obj
*.bc
*.exe

# LaTeX build products / LaTeX 构建产物
report/build/
report/*.aux
report/*.bbl
report/*.blg
report/*.fdb_latexmk
report/*.fls
report/*.log
report/*.out
report/*.pdf
report/*.toc
